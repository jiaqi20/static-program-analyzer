procedure p {
  x = x + 1;
  y = x + y;
  if (x<0) then {
    while (i>0) {
      x = z * 3 + 2 * y;
      call q;
      i = i - 1; }
    x = x + 1;
    z = x + z; }
  else {
    z = 1; }
  z = z + x + i;
  read x;
  print x;}

procedure q {
  
}